<!DOCTYPE html>
    <html lang="en" data-bs-theme="light">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>keep</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
        <link rel="stylesheet" href="styles.css">
    </head>
    <body class="d-flex align-items-center py-4 bg-body-tertiary">
        <main class="w-75 m-auto">
            <div id="header">
                <h1 class="title" id="heading">Welcome to keep</h1>
                <% if (locals.projects.length > 0) { %>
                    <p class="lead">Create a new project or select one below</p>
                    <button id="new-project-btn" class="bg-pink btn btn-primary btn-lg" data-bs-toggle="modal" data-bs-target="#createProjectForm"><i class="bi bi-plus"></i> New Project</button>
                    <div id="projects" class="d-flex flex-wrap">
                        <% for( let i = 0; i < locals.projects.length; i++ ) { %>
                            <div class="card me-2 mt-5 bg-pink" style="width: 18rem;">
                                <a href="/projects?id=<%= locals.projects[i].id %>" id="anchorCard">
                                    <div class="card-body">
                                        <i class="bi <%= locals.projects[i].icon %> mb-5 vivid-pink large-icon"></i>
                                        <h3 class="card-title"><%= locals.projects[i].title %></h3>
                                        <p class="card-text mb-2"><%= locals.projects[i].desc %></p>
                                    </div>
                                </a>
                            </div>
                        <% } %>
                    </div>
                <% } else { %>
                    <p class="lead">Create a new project to begin</p>
                    <button id="new-project-btn" class="btn btn-primary btn-lg bg-pink" data-bs-toggle="modal" data-bs-target="#createProjectForm"><i class="bi bi-plus"></i> New Project</button>
                    <div id="projects"></div>
                <% } %>
                <div id="createProjectForm" class="modal fade" tabindex="-1" aria-labelledby="createProjectForm">
                    <div class="modal-dialog modal-dialog-centered modal-lg">
                        <div class="modal-content rounded-4 shadow">
                            <div class="modal-header p-5 pb-4 border-bottom-0">
                                <h1 class="fw-bold mb-0 fs-2">Create a new project</h1>
                                <button class="btn-close" type="button" data-bs-dismiss="modal" aria-label="close"></button>
                            </div>
                            <form action="/new-project" method="post" enctype="multipart/form-data">
                                <div class="modal-body p-5 pt-0 d-flex">
                                    <div class="container">
                                        <div class="row">
                                            <div class="col">
                                                <div class="form-floating">
                                                    <input type="text" name="title" class="form-control" id="floatingTitle" placeholder="New Project">
                                                    <label for="floatingTitle">New Title</label>
                                                </div>
                                                <div class="form-floating">
                                                    <input type="text" name="desc" class="form-control" id="floatingDesc" placeholder="A short description">
                                                    <label for="floatingDesc">A short description</label>
                                                </div>
                                            </div>
                                            <div class="col">
                                                <div id="dropzone" class="box__input">
                                                    <input class="box__file" type="file" name="files" id="file" data-multiple-caption="{count} files selected" multiple />
                                                    <label for="file"><strong >Choose files</strong><span class="box__dragndrop"> or drag them here</span></label>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                <div class="modal-footer">
                                    <button type="submit" class="btn btn-primary bg-pink" data-bs-dismiss="modal">Create Project</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </main>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js" integrity="sha384-j1CDi7MgGQ12Z7Qab0qlWQ/Qqz24Gc6BM0thvEMVjHnfYGF0rmFCozFSxQBxwHKO" crossorigin="anonymous"></script>
        <script src="app.js"></script>
    </body>
</html>
